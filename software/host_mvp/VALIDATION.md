# Host MVP v0.3 验证记录

## v0.6 SW-07 阶段验证（2026-09-22）

全套41项测试通过，21.617秒。新用例以已知数列核对基线/稳定均值、OLS斜率、均值差和失水；验证质量/缺值/禁用通道排除索引、重启/倒序/重复与迟到帧补齐、重复实验条件限制、零均值CV为null、两次报告指标与哈希一致、原目录不变、版本目录不覆盖、未完成批次/源文件变化拒绝导出。已导出两批模拟演示报告并检查Matplotlib图表。`git diff --check`通过。没有真实实验结论，未完成硬件或长期验收。

## v0.5 SW-06 阶段验证（2026-09-22）

同v0.4环境，全套 `unittest discover` 36项通过，19.826秒。新增新旧批次、带时区日期回退、时间倒退/非法值、缺图和路径越界、多批次跨启动时间对齐、事件次数缺失、标签修订游标及文件哈希不变测试；Qt测试验证曲线窗口、事件跳转与播放状态。已用offscreen渲染并检查中文布局。未进行真实批次数据或大规模内存性能验收。

## v0.4 SW-04 阶段验证（2026-09-22）

Windows / Python 3.12.14 / PySide6 6.11.2，运行 `python -m unittest discover -s software/host_mvp -v`：32项通过，20.409秒。新增档案未知值与非法质量、追溯字段覆盖拒绝、档案导入/校验/禁止覆盖、批次快照独立性、设备配置历史、初始质量来源、GUI编辑验证；桌面集成验证批次中锁定档案及下一批清空质量。原断线用例改用有期限的状态等待，避免200毫秒固定等待误判。`git diff --check`通过。

未连接真实设备；人工标定记录不代表设备精度通过，原实物验收边界仍有效。以下保留v0.3历史记录。

日期：2026-09-22。环境：Windows、Python 3.14.4、项目已有虚拟环境。对应版本由包含本文件的Git提交确定。

## 自动检查

```powershell
software/host_mvp/.venv/Scripts/python.exe -m unittest discover -s software/host_mvp -v
```

结果：**28项通过，16.875秒**。Qt桌面测试使用offscreen模式，数据全部写入临时目录。

- 原有12项回归：批次路径/覆盖保护、模拟界面与多批次、线程生命周期、设备质量标记及字段、缺失/异常/通道掩码和完整性报告。
- 新增16项：命令执行中与完成分离、气路接受/拒绝、动作超时及迟到应答、旧设备信息查询超时兼容；没有Qt事件循环时后台采集持续落盘；批次外标定日志；无效UTF-8原始字节保存；慢写入队列溢出及接收/写入计数；模拟磁盘故障；模拟图片与相机失败；等待照片收尾及旧回调隔离；标签未知值及追加修订；设备身份变化终止批次；序号复位使待执行命令失效；分段串口帧、断线重连且动作不重放；缺图及事件引用检查。
- `git diff --check`通过。测试文件显式使用UTF-8读取日志；连接测试等待握手状态完成，避免固定延时造成不稳定。

## 验证边界

本轮没有接真实ESP32-S3、传感器或摄像头，未重新编译硬件固件（硬件代码未改动）。模拟标定仅模拟状态响应，不模拟真实称重误差或NVS。

尚待验收：真实串口拔插、设备供电复位、泵阀互锁、称重标定持久化与多点误差、真实相机拔插/驱动阻塞、固定曝光和白平衡、磁盘实际写满/断电恢复、至少8小时连续联机。当前相机线程无法强制中断阻塞的原生驱动；退出会等待尚未返回的照片任务。

关闭窗口或结束批次不是物理急停，真实气路应先发停止命令并核对设备；硬件安全链和有限时保护仍须独立验收。模拟数据和模拟图像不得用于实验或算法性能结论。
